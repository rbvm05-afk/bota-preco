"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { AppShell } from "@/components/AppShell";
import { money } from "@/lib/calculator";
import { clearCalculations,getCalculations,type SavedCalculation } from "@/lib/storage";
export default function HistoricoPage(){const[items,setItems]=useState<SavedCalculation[]>([]);useEffect(()=>setItems(getCalculations()),[]);const clear=()=>{clearCalculations();setItems([])};return <AppShell compact><div className="animate-rise"><div className="mb-7 flex items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.16em] text-[var(--green)]">Seus produtos</p><h1 className="mt-2 text-4xl font-black tracking-[-.04em]">Meus cálculos</h1><p className="mt-2 text-[var(--muted)]">Tudo o que você já precificou fica organizado aqui.</p></div>{items.length>0?<button onClick={clear} className="rounded-xl px-3 py-2 text-sm font-black text-[var(--red)] hover:bg-red-50">Limpar tudo</button>:null}</div>{items.length===0?<div className="surface rounded-[2rem] p-9 text-center"><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-[var(--green-soft)] text-2xl">🧾</span><h2 className="mt-5 text-2xl font-black">Ainda não há nada guardado.</h2><p className="mt-3 text-[var(--muted)]">Quando você terminar uma conta, ela aparece aqui.</p><Link href="/" className="mt-6 inline-block rounded-2xl bg-[var(--green)] px-6 py-4 font-black text-white">Fazer meu primeiro cálculo →</Link></div>:<div className="space-y-4">{items.map(item=><article key={item.id} className="surface rounded-3xl p-5 transition hover:-translate-y-0.5"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-[var(--green-soft)] px-3 py-1 text-[10px] font-black uppercase tracking-[.14em] text-[var(--green)]">{item.mode === "rapidin" ? "Rapidin" : "Completão"}</span><h2 className="mt-3 text-xl font-black">{item.input.productName}</h2><p className="mt-1 text-sm text-[var(--muted)]">{new Date(item.createdAt).toLocaleString("pt-BR")}</p></div><div className="rounded-2xl bg-[var(--green-deep)] px-4 py-3 text-right text-white"><span className="block text-xs text-white/65">Preço verde</span><strong className="mt-1 block text-xl">{money(item.result.healthyPrice)}</strong></div></div></article>)}</div>}</div></AppShell>}
